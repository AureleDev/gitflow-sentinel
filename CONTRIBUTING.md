# Contributing

Create a short-lived branch from `main`, keep changes focused, and open a pull request.

## Local checks

- `npm run verify`
- `npm run validate:evals`
- `npm run validate:package`
- `npm run validate:self-host`

Never commit credentials or local environment files. Report security issues using `SECURITY.md`.
