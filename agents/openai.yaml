interface:
  display_name: "Gitflow Sentinel"
  short_description: "Audit and migrate existing Sentinel guardrails"
  default_prompt: "Use $gitflow-sentinel to audit this existing Sentinel installation and migrate it safely to the transactional configure-project workflow."
