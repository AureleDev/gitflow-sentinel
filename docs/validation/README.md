# Validation evidence

This directory contains dated, repository-only evidence from controlled test
runs. It is not part of the Gitflow Sentinel runtime and is excluded from the
npm package.

Validation that can modify files must use disposable copies outside source
repositories. A report records observed results and limitations; it is never a
substitute for the automated test suite or current CI status.
