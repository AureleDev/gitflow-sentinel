interface:
  display_name: "Gitflow Sentinel"
  short_description: "Configure or migrate project foundations safely"
  default_prompt: "Use $gitflow-sentinel to inspect this project, explain the recommended setup, and prepare a reversible configuration plan."
